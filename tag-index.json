---
layout: none
---
{
    {% for post in site.posts %}
      "{{ post.url | slugify }}": {
        "tags": "{{ post.tags | xml_escape }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
}